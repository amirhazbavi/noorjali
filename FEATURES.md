# قابلیت‌های افزوده‌شده بدون حذف نسخهٔ قبلی

`index.html` و قابلیت‌های قبلی حفظ شده‌اند. فایل‌های تکمیلی این نسخه:

- `manifest.json`: نصب برنامه به‌صورت PWA.
- `sw.js`: cache پوستهٔ برنامه برای حالت آفلاین پایه.
- `share-ayah.html`: تولید، دانلود و اشتراک‌گذاری تصویر آیه.

قابلیت‌های اصلی موجود در برنامه شامل تفسیر، دعاها، اوقات شرعی و قبله، تسبیح، جزء/سوره/صفحه، پخش و هایلایت آیه، سرعت و تکرار صوت، ادامه از آخرین صفحه، جستجوی متن، اندازه فونت و ردیاب ختم است.

برای فعال‌شدن PWA، سایت را از طریق HTTPS مانند GitHub Pages باز کنید؛ service worker روی `file://` اجرا نمی‌شود.
