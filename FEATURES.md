# تغییرات اخیر

قابلیت‌های اصلی موجود در `index.html` شامل جستجوی متن، ادامه از آخرین صفحه، نشانه‌گذاری ختم، تغییر اندازه متن، پخش و تکرار تلاوت، هایلایت آیه، اوقات شرعی، قبله، ادعیه، تسبیح دیجیتال و فهرست جزء است.

همچنین برای تکمیل قابلیت نصب و آفلاین:
- `manifest.json` برای نصب به‌صورت PWA اضافه شده است.
- `sw.js` برای cache پوستهٔ برنامه و پشتیبانی پایه از حالت آفلاین اضافه شده است.
- `mojtaba-khamenei.html` تصویر آیت‌الله سید مجتبی خامنه‌ای را با ذکر منبع و مجوز نمایش می‌دهد.

برای فعال‌شدن service worker، صفحه را از طریق HTTPS (مانند GitHub Pages) باز کنید؛ service worker روی `file://` فعال نمی‌شود.
