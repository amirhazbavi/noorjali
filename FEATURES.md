# قابلیت‌های تلاوت نور

هیچ بخشی از برنامهٔ قبلی حذف نشده است. صفحهٔ اصلی `index.html` امکانات قرآن، ترجمه، تلاوت، جستجو، دعاها، اوقات شرعی، قبله، تسبیح، هایلایت و ردیاب ختم را حفظ می‌کند.

فایل‌های افزوده:
- `manifest.json`: نصب به‌صورت PWA.
- `sw.js`: cache پوسته برای آفلاین پایه.
- `share-ayah.html`: ساخت و اشتراک تصویر آیه.
- `features.html`: مرکز امکانات، نشان‌گذاری شخصی، ردیاب پیشرفت روزانه و تغییر حالت روشن/تیره.

برای اجرای کامل PWA، سایت را از طریق HTTPS مانند GitHub Pages باز کنید.
