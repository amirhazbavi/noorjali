# قابلیت‌های تلاوت نور

قابلیت‌های اصلی درخواست‌شده در `index.html` موجود هستند: تفسیر، ادعیه، اوقات شرعی و قبله، تسبیح، ناوبری جزء، پخش و هایلایت آیه، سرعت و تکرار، آخرین صفحه، جست‌وجوی متن، اندازه فونت و ردیاب ختم.

موارد تکمیلی اضافه‌شده:
- `manifest.json`: نصب به‌صورت PWA.
- `sw.js`: cache پوستهٔ برنامه برای حالت آفلاین پایه.
- `share-ayah.html`: ساخت و دانلود تصویر آیه و اشتراک‌گذاری متن.
- `mojtaba-khamenei.html`: نمایش تصویر با ذکر منبع و مجوز CC BY 4.0.

برای PWA، سایت باید با HTTPS (مثل GitHub Pages) باز شود؛ service worker روی `file://` فعال نیست.
